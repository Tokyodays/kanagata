machine:
  node:
    version: v6.9.4

dependencies:
  post:
    - npm install -g gulp
    - npm install
    - npm rebuild node-sass
    - gulp

deployment:
  production:
    branch: master
    heroku:
      appname: kanagata
  staging:
    branch: master
    heroku:
      appname: kanagata
