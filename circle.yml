machine:
  node:
    version: v6.9.4

dependencies:
  post:
    - gulp -v
    - npm rebuild node-sass
    - gulp

deployment:
  production:
    branch: master
    heroku:
      appname: kanagata
  staging:
    branch: master
    heroku:
      appname: kanagata
